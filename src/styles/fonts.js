import { css } from 'styled-components';

const fontSmall = css`
  font-size: 0.8rem;
`;

const fontSmallBold = css`
  font-size: 0.8rem;
  font-weight: bold;
`;

const fontLarge = css`
  font-size: 1.2rem;
`;

const fonts = {
  fontSmall,
  fontSmallBold,
  fontLarge,
};

export default fonts;
