import { css } from 'styled-components';

const fontSmall = css`
    font-size: 0.8rem;
`;

const fontSmallBold = css`
    font-size: 0.8rem;
    font-weight: bold;
`;

const fonts = {
    fontSmall,
    fontSmallBold,
};

export default fonts;
